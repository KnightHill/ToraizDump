#!/bin/sh

project_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
launcher="$project_dir/.venv/bin/toraiz-programs"

if [ ! -x "$launcher" ]; then
    echo "toraiz-programs: project launcher not found; run: .venv/bin/python -m pip install -e ." >&2
    exit 1
fi

exec "$launcher" "$@"
